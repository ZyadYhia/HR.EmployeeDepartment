@model CreateDepartmentDto
@{
    ViewData["Title"] = "Create Dpt Page";
}

<div class="">
    <h1 class="display-4">Create New Department</h1>
    <form asp-action="Create" method="post">
        <partial name="EmployeePartialView/_CreateEditPV" model="@Model" />
        <div class="mt-4">
            <button type="submit" class="btn btn-primary">Create</button>
            <a class="btn btn-secondary" asp-controller="Department" asp-action="Index">Cancel</a>
        </div>
    </form>
</div>